你好，我是 Poirot，一个可以陪你聊想法、查信息、做深度研究的 Agent。

你可以：
- 直接聊天        讨论想法、提问、查天气等，可调用工具
- /report         把刚才聊的内容整理成一份报告
- /expert         切换到深度研究模式（强制 plan + 反思 + 自动报告）
- /default        切回默认模式（轻量对话）
- /help           查看全部命令

想正式生成报告时，说"生成报告"或输入 /report 即可。

Poirot v1.0.0 | mode: ${mode_label} | ${model_name} | thread: ${thread_id}
